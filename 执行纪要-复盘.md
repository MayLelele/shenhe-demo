# 执行纪要（复盘用）

> 目标：记录「审核辅助 Agent / 审核台 / 静态展示」相关变更与排障，便于后续复盘。  
> 时间轴：以会话内工作为准（约 2026-04-22 ～ 2026-04-23，本地时区以机器为准）。

---

## 1. 背景与范围

- 核心代码在 `audit_agent/`，与 `main.py` 同入口的交互、与 `Agent` 一致的工具、记忆、反思链。
- 产出一类**静态总览**、一类**本机 Web 审核台**（需自行启动 `audit_web.py`）。

---

## 2. 已交付的工件与作用

| 文件 | 作用 |
|------|------|
| `audit_agent-overview.html` | 项目结构 + README 化说明 + 各源码全文的静态总览，浏览器直接打开。 |
| `审核员-main审核可视化.html` | 单次工单示例的「人工审核员视角」页面：与 `main` 实跑/工具链对齐的结论区 + 证据区（演示规则库须与正式内控对账前为准）。 |
| `audit_workbench.html` + `audit_web.py` | 本机 `GET /` 提供表单页；`POST /api/audit` 用 `AuditAgent` 走一轮，返回可渲染的**判定摘要、完整答复、证据链、validate 解析快照**。环境变量与 `main` 相同（`.env` 中 `OPENAI_API_KEY` 等）。 |
| `audit_agent/agent.py` 增补 | Mock 下工具结果写入 `memory`，与线上一致，便于「证据链」不丢步；`collect_evidence_chain()` 供 API 取序。 |
| `audit_web.py` 内 `_infer_verdict` | 从答复里抽**标签**；优先 `【倾向结论】`，并对「工具/规则句里的 则倾向…」做去噪，减少误判。 |

---

## 3. 运行与验证命令（摘要）

```bash
cd audit_agent
source .venv/bin/activate   # 若已建虚拟环境
pip install -r requirements.txt
```

- CLI：`python main.py`（真实） / `python main.py --mock`（离线结构演示）。
- 审核台：`python audit_web.py` → 浏览器 `http://127.0.0.1:8765/`。  
- 无 Key 时：表单勾选 **Mock**，或配置 `.env` 后再用真实模式。

排障：若 `Address already in use`，说明默认端口 `8765` 已被占用。可 `lsof -i :8765` 查 PID 后结束进程，或设 `AUDIT_WEB_PORT=8766` 再启。

---

## 4. 与外部工具无关的侧记

- Cursor 侧 **Lark MCP** 若报 `-32001` 超时，多属连接/网络/鉴权，与本仓库代码无强绑定；需在 Cursor 的 MCP 状态里自排查。

---

## 5. 已知限制（复盘时请带上）

- 规则库为**演示片段**，**正式以业务制度与系统数据为准**；Agent 与页面均为辅助，不替代内控签批。
- 反思分支在部分返回下可能以「反思全文」为主展示，**初稿**未必单独出现在同一段里（与 `agent._reflect` 的拼接策略有关）——以终端/Web 中最终 `reply` 文本为准做归档。
- `verdict` 为从文本**启发式**抽取的快捷标签，与法条/内部口径不一致时，以全文结论人工复核。

---

## 6. 建议的下次行动（可选）

- 将 `POST /api/audit` 的请求体/响应在 OpenAPI 或同目录 `README` 中再收一行字段说明（若对多人协作有需求）。  
- 为 `audit_web` 加 `--port` 或把端口写入 `.env.example` 示例，减少端口冲突的口头传递成本。  
- 需要正式归档时，可将本纪要与发布记录一并放入团队 Wiki，并标出**演示规则**与**生产**边界。

---

## 7. 审核知识库（延续维护）

- 以 [`knowledge_base/README.md`](./knowledge_base/README.md) 为总入口，内含规则条目、工单字段、系统映射与**新增条目的模板/流程**；与 `POLICY_RULES`、SKILL 沉淀路径已写清。

---

*本文件为过程纪要，不替代产品需求与合规审计结论。*
